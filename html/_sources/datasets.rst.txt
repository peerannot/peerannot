.. _datasets-documentation:

==========================
Datasets
==========================

We introduce a format for crowdsourced datasets that can be reused in multiple contexts.

Each dataset is a folder with the following architecture:

TODO: present the structure